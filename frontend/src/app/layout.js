import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Dapoerku - Restoran Tradisional Modern",
  description: "Dashboard manajemen Restoran Dapoerku - Kelola menu, pesanan, dan operasional restoran dengan mudah",
  keywords: "restoran, dapoerku, kuliner, makanan tradisional, dashboard",
};

export default function RootLayout({ children }) {
  return (
    <html lang="id">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
